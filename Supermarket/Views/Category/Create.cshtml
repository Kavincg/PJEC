@using Supermarket.Models
@model Category
@{
    // 'existingImageUrl' is not needed in the Create view as there's no existing image.
    // Kept for consistency if copied from an Edit view, but it will always be empty here.
    string existingImageUrl = "";
}

<div class="container py-5">
    <div class="card shadow-lg border-0 rounded-3">
        <div class="card-header bg-gradient-header text-white p-4 rounded-top-3">
            <div class="row align-items-center">
                <div class="col-12 text-center text-md-start">
                    <h3 class="mb-0 text-white">Create New Category</h3> @* Corrected title *@
                </div>
            </div>
        </div>
        <div class="card-body p-4">
            <form method="post" class="row g-3" enctype="multipart/form-data">
                <div asp-validation-summary="All" class="text-danger mb-3"></div>

                <div class="col-md-8">
                    <div class="form-floating mb-3">
                        <input asp-for="Name" class="form-control border-secondary rounded-2" placeholder="Category Name" />
                        <label asp-for="Name" class="text-muted">Category Name</label>
                        <span asp-validation-for="Name" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="form-floating mb-3">
                        @* Ensure this is type="number" for DisplayOrder *@
                        <input asp-for="DisplayOrder" type="number" class="form-control border-secondary rounded-2" placeholder="Display Order" />
                        <label asp-for="DisplayOrder" class="text-muted">Display Order</label>
                        <span asp-validation-for="DisplayOrder" class="text-danger small mt-1 d-block"></span>
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-muted mb-2">Category Logo</label> @* Changed label to Logo *@
                        @* CRITICAL CHANGE: Added name="file" to match the IFormFile parameter in the controller *@
                        <input class="form-control border-secondary rounded-2" type="file" accept="image/*" id="imageUpload" name="file" />
                    </div>

                    <div class="col-12 d-flex justify-content-end gap-2 mt-4">
                        <button type="submit" class="btn bg-gradient-header px-4 py-2 text-white">
                            @* Added text-white for styling *@
                            <i class="bi bi-plus-circle me-2"></i> Create
                        </button>
                        <a asp-controller="Category" asp-action="Index" class="btn btn-outline-secondary px-4 py-2">
                            @* Corrected controller *@
                            <i class="bi bi-arrow-left-circle me-2"></i> Back to List
                        </a>
                    </div>
                </div>

                <div class="col-md-4 d-flex align-items-center justify-content-center">
                    <div class="image-preview-container border p-2 text-center" style="min-height: 200px; width: 100%; display: flex; align-items: center; justify-content: center; background-color: #f8f9fa;">
                        @* For Create, the initial image source is empty and hidden, text is visible *@
                        <img id="imagePreview" src="#" alt="Category Image" class="img-fluid" style="display:none; max-height: 250px; object-fit: contain;" />
                        <span id="noImageText" class="text-muted">No image selected</span>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        document.getElementById('imageUpload').addEventListener('change', function (event) {
            const [file] = event.target.files;
            const preview = document.getElementById('imagePreview');
            const noImageText = document.getElementById('noImageText');

            if (file) {
                preview.src = URL.createObjectURL(file);
                preview.style.display = 'block';
                noImageText.classList.add('d-none');
            } else {
                preview.src = "";
                preview.style.display = 'none';
                noImageText.classList.remove('d-none');
            }
        });
    </script>
}

<style>
    /* Add the same style block as your Create.cshtml to ensure consistent styling */
    :root {
        --orange-main-color: rgb(234,80,28); /* The primary orange from your header */
        --orange-line-color: rgba(234,80,28,0.5); /* A lighter shade for the lines (if needed elsewhere) */
    }

    .bg-gradient-header {
        background-color: var(--orange-main-color);
    }

    .text-gray-700 {
        color: #495057;
    }

    .text-black {
        color: #000;
    }

    .text-3xl {
        font-size: 1.875rem;
        line-height: 2.25rem;
    }

    .font-bold {
        font-weight: 700;
    }

    .rounded-4 {
        border-radius: 1rem !important;
    }

    .rounded-top-4 {
        border-top-left-radius: 1rem !important;
        border-top-right-radius: 1rem !important;
    }

    .rounded-pill {
        border-radius: 50rem !important;
    }

    .hover-scale-105:hover {
        transform: scale(1.05);
    }

    .transition-all {
        transition-property: all;
        transition-duration: 200ms;
        transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }

    .card.shadow-lg {
        box-shadow: 0 1rem 3rem rgba(0,0,0,.1) !important;
    }

    .card-body.p-4 {
        padding: 1.5rem !important;
    }

    .form-control.rounded-pill {
        border-radius: 50rem !important;
        padding-left: 1.25rem;
        padding-right: 1.25rem;
    }

    .form-floating > .form-control.rounded-pill,
    .form-floating > .form-control.rounded-pill ~ label {
        height: calc(3.5rem + 2px);
    }

    .form-floating > label {
        padding: 1rem 1.25rem;
    }

    .form-floating > .form-control.rounded-pill:focus,
    .form-floating > .form-control.rounded-pill:not(:placeholder-shown) {
        padding-top: 1.625rem;
        padding-bottom: 0.625rem;
    }

    .btn.bg-gradient-header {
        background-color: var(--orange-main-color);
        color: #fff;
        border: none;
    }

        .btn.bg-gradient-header:hover {
            background-color: var(--orange-main-color);
            opacity: 0.9;
        }

    .btn-secondary {
        background-color: #6c757d;
        border-color: #6c757d;
        color: #fff;
    }

        .btn-secondary:hover {
            background-color: #5a6268;
            border-color: #545b62;
            color: #fff;
        }
</style>