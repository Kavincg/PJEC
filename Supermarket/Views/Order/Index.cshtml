@model List<OrderHeader>

@{
    ViewData["Title"] = "Order List";
}

<div class="card shadow-lg border-0 mt-5 mb-4 rounded-4">
    <div class="bg-gradient-header text-white py-4 px-4 rounded-top-4">
        <div class="row align-items-center">
            <div class="col-12 text-center">
                <h2 class="mb-0 text-3xl text-black font-bold">Order List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="table-responsive">
            <table class="table table-bordered table-striped table-hover align-middle">
                <thead class="table-header-custom text-black">
                    <tr>
                        <th class="text-black" scope="col">User Name</th>
                        <th class="text-black" scope="col">PhoneNo</th>
                        <th class="text-black" scope="col">Payment ID</th>
                        <th class="text-black" scope="col">Order Date</th>
                        <th class="text-black" scope="col">Order Cost</th>
                        <th class="text-black" scope="col">Order Details</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var obj in Model.OrderByDescending(h => h.OrderDate)) 
                    {
                        @if (!(string.IsNullOrEmpty(obj.PaymentIntentId))){
                            <tr>
                                <td>@obj.Name</td>
                                <td>@obj.Phone</td>
                                <td>@obj.PaymentIntentId</td>
                                <td>@obj.OrderDate.ToString("yyyy-MM-dd HH:mm")</td>
                                <td>@obj.OrderTotal.ToString("C", System.Globalization.CultureInfo.GetCultureInfo("en-IN"))</td>
                                <td>
                                    <div class="d-grid">
                                        <a asp-controller="ShoppingCart" asp-action="OrderConfirmation" asp-route-id="@obj.Id"
                                           class="btn bg-gradient-header btn-sm rounded-pill px-3 py-2 shadow-sm text-white hover-scale-105 transition-all">
                                            <i class="bi bi-info-circle me-1"></i> Details
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script>
    </script>
}

<style>
    :root {
        --orange-main-color: rgb(234,80,28); 
        --orange-line-color: rgba(234,80,28,0.5);
    }

    .bg-gradient-header {
        background-color: var(--orange-main-color);
    }

    .text-gray-800 {
        color: #343a40;
    }

    .text-gray-600 {
        color: #6c757d;
    }

    .text-gray-700 {
        color: #495057;
    }

    .text-orange-600 {
        color: #ea501c;
    }

    .text-3xl {
        font-size: 1.875rem;
        line-height: 2.25rem;
    }

    .font-bold {
        font-weight: 700;
    }

    .font-semibold {
        font-weight: 600;
    }

    .font-medium {
        font-weight: 500;
    }

    .rounded-4 {
        border-radius: 1rem !important;
    }

    .rounded-top-4 {
        border-top-left-radius: 1rem !important;
        border-top-right-radius: 1rem !important;
    }

    .hover-scale-105:hover {
        transform: scale(1.05);
    }

    .transition-all {
        transition-property: all;
        transition-duration: 200ms;
        transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }

    .table-header-custom {
        background-color: var(--orange-main-color);
        color: #fff; 
    }

        .table-header-custom th {
            border-color: rgba(255, 255, 255, 0.3) !important; 
            padding: 0.75rem 1rem; 
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.9rem;
        }

    .table-bordered {
        border-color: #dee2e6;
    }

        .table-bordered th,
        .table-bordered td {
            border-color: #dee2e6;
        }

    .table th, .table td {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        padding: 0.6rem 1rem; 
        font-size: 0.95rem; 
    }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: #f8f9fa; 
    }

    .table-hover tbody tr:hover {
        background-color: #e2e6ea; 
        cursor: pointer;
    }

    .card.shadow-lg {
        box-shadow: 0 1rem 3rem rgba(0,0,0,.1) !important; 
    }

    .card-body.p-4 {
        padding: 1.5rem !important; 
    }

    .btn .bi {
        vertical-align: middle; 
    }
</style>